name: qdev
version: "0.2.0"
config-version: 2

profile: qdev

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
seed-paths: ["seeds"]

clean-targets:
  - target
  - dbt_packages

models:
  qdev:
    staging:
      +materialized: view
      +schema: staging
