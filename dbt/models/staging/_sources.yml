version: 2

sources:
  - name: raw
    description: Untransformed data loaded by the Python ingest pipeline.
    schema: raw
    tables:
      - name: prices
        description: Daily OHLCV prices from market data providers (yfinance).
